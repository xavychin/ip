package leo.display;

import javafx.application.Application;

/**
 * The Launcher class is the main entry point to the application.
 * It is also used as a workaround to classpath issues.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
